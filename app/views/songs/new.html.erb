<h1>Add a new review:</h1>

<%= form_for [@product, @review] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :content_body %>
  <%= f.text_area :content_body %>
  <%= f.label :rating %>
  <%= f.text_field :rating %>
  <p><%= f.submit %></p>
<% end %>